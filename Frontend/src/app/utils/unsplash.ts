export async function unsplash_tool(query: string): Promise<string> {
  // This is a placeholder that would normally call the actual unsplash_tool
  // For now, return a placeholder image
  return `https://images.unsplash.com/photo-1540575467063-178a50c2df87?w=800&q=80`;
}
